# 更新日志

## 2026-09-10（仅保留桌面版源码）

- 正式纳入 Electron 主进程、内置 Express 服务、原生模块界面、依赖锁文件和合成数据回归测试。
- 移除旧 Flask/Jinja Web 版、PyInstaller 打包脚本、迁移对拍与调试脚本，以及已被当前规范替代的旧说明；历史实现仍可通过 Git 历史查看。
- 保留桌面必需的 `static/frontend/`、五种筛选规则、旧桌面会话迁移及 Excel 导出，不再依赖旧 Python 应用。
- 启动和所有架构打包命令自动生成图标，并将窗口图标收入应用；新增无预生成资产的图标与仓库结构回归测试。
- 使用合成数据五模式 API 回归替代依赖本地业务 Excel 的旧冒烟脚本，补齐首次安装和测试说明。
- 更新开发规范与忽略规则，真实业务数据、个人笔记、依赖、缓存和历史安装包保持本地，不删除、不上传。

## 2026-09-09（统一 UI 与模块结构）

- 以夜间停留页为基准统一页面标题、内容面板、按钮、字段、选择器、提示、文件选择和表格分页，建立共享设计变量与分层 CSS。
- 日期、时间和日期时间改为自定义弹层，支持中文月历、年月切换、24 小时制、键盘操作和直接填写；确认才应用，取消/外部点击/Esc 不改变原值。
- 单选地点替换原生 datalist，多选使用统一公共组件；删除改为可访问的页面内确认框，系统文件打开与保存保持原生。
- 表单内容与底部操作分开布局，移除遮挡文字的悬浮操作栏；增加弹层层级、视口避让和焦点恢复。
- 前端迁移为无构建步骤的原生 ES Modules，拆分页面、视图、控件、请求、路由和纯逻辑；公共控件拥有明确的挂载/更新/销毁生命周期。
- 本地服务拆分 HTTP 路由、参数处理与业务服务，五种模式通过显式调度调用原有算法；HTTP 契约、草稿键、业务规则和工作簿内容保持不变。
- 拆分测试夹具和浏览器助手，修复测试进程的连接清理/输出管道超时；保留所有原回归断言，新增公共控件与缩放布局用例。

## 2026-09-09（可选同一夜间窗口）

- 夜间停留继续允许在设定时段内任意时刻进入，包括凌晨，不采用“只能 19:00 后进入”的脚本口径。
- 新增“进入和驶出必须在同一夜间窗口”复选项，默认开启；关闭后允许跨白天或多天停留，但进出时刻仍须各自落在设定时段内。
- 根据实际进入时间确定窗口起止日期，修复仅比较钟点导致跨白天/多天误命中的问题；两类单边复核记录不受此开关影响。
- 窗口边界按真实时刻判断，时长按原始差值严格比较，仅在展示时四舍五入。
- 布尔选项在草稿、已执行条件、会话重启及导出中一致保留。历史结果不自动重算，缺少选项时明确标为旧版未限制。
- 新增合成数据回归测试，覆盖凌晨进入、自定义日内时段、跨白天/多天、精确边界、显式 false 恢复、导出与界面交互。

## 2026-09-09（功能首页与专用功能页重组）

- 用“你想查什么车辆？”的五个直白功能入口替换原三步导航，数据、条件和车辆结果均在对应功能内完成；支持哈希路由和浏览器前进/后退。
- 一份通行文件供各功能复用，地点自动从文件识别，不再要求先维护卡口库；名单可在名单查询页内直接导入。
- 只展示当前功能必要条件，提供全天/夜间/自定义时段，专业设置收进“更多设置”，参考通行时间的排序语义明确说明。
- 结果默认按车牌一车一行，支持车牌/姓名搜索及单车详情；夜间三类结果分别汇总，零命中不隐藏复核记录。
- 按功能原子保存执行条件与结果快照，下载明确指定功能；一个功能的执行、草稿或库维护不会覆盖另一个功能的结果。
- v1 草稿迁移至 v2 按功能隔离的配置；旧会话和未带 mode 的旧接口兼容保留。
- 增加完整记录跨页聚合、五功能共存及持久化、自动地点识别、真实 Electron 操作和最小窗口入口可见性回归测试。

## 2026-09-09（页面操作逻辑优化）

- 修复普通表单与接口编码不匹配；配对页面改用后台实际生效的日内时段，隐藏模式不参与提交或校验。
- 按批次、模式保留草稿，完善会话恢复、失效选项提示、流程导航、请求竞态和重复提交保护。
- 上传队列支持单独移除、数量/大小检查及失败重试；库管理按来源返回，删除前确认数量。
- 成功筛选保存独立条件快照；失败不覆盖上次结果，分页显示实际页码，零命中仍显示夜间复核表并可导出。
- 修正独立服务静态资源入口；增加 Windows 临时文件占用重试，并补充 pytest + 隐藏 Electron 的合成数据回归测试。
- 保持五种筛选算法、旧 Flask 页面和 Excel 工作表结构不变。

## 2026-09-08（夜间停留模式）

### 新增：夜间停留筛查（第五种模式，night_stay）

- 筛选模式新增「夜间停留」卡片，全部参数页面自定义：
  - 进口卡口 / 出口卡口多选（来自卡口库，带搜索/全选）
  - 筛选日期范围（含两端）、夜间窗口（默认 19:00~次日 05:00，支持跨午夜）
  - 最短停留时长（分钟，默认 60，严格大于）
- 配对算法（严格相邻 + 孤儿记录保留）：每车牌全部记录按时间排序后顺序扫描——
  - 遇进口 → 覆盖为当前进入记录（连续进口只保留最后一条）
  - 遇出口 → 有当前进入记录则配对为一次通行并清空；无则记为「无进入有驶出」孤儿（连续多条出口逐条记录，备注标明序号）
  - 扫描结束仍有未配对进入 → 记为「有进入无驶出」
- 命中口径：进入时间 ∈ 日期范围（含两端）、停留完全落在夜间窗口（进入与驶出时刻均在窗口内）、时长 > 阈值；两张复核表与命中口径对称（时间落日期范围 + 夜间窗口）
- 无牌车/未识别车牌在解析阶段剔除（复用现有逻辑）
- 结果页四段展示：可疑车辆汇总（按命中次数降序）、停留明细（含进/出口图片 URL）、有进入无驶出、无进入有驶出
- 导出 Excel 5 个工作表：汇总（参数回显 + 各卡口统计 + 方向命中数 + 孤儿数）、可疑车辆汇总、停留明细（带自动筛选）、有进入无驶出、无进入有驶出（含连续出口序号备注）
- 测试：`scripts/night-stay-test.js`（36 项边界断言：跨午夜命中、时长边界、窗口边缘整点、连续进口覆盖、连续孤儿序号、日期边界、无关卡口跳过、汇总统计）+ `scripts/night-stay-e2e.js`（13 项断言：API 全流程 + 5 工作表导出核对 + 无牌车剔除）
- 会话持久化支持 night_stay 结果对象（含内嵌原始记录的 Date 序列化/还原）

## 2026-09-08

### 重大变更：桌面化（Electron 22 + Node 后端）

- 新增 Windows 桌面软件形态，替代原"本地 Web 服务 + 浏览器"模式：
  - Electron 22.3.27（Chromium 108，官方支持 Win7 的最后版本线）原生窗口
  - Node/Express 后端完整重写原 Flask 逻辑，API 契约与旧版逐字段一致
  - 双击即用，无浏览器、无控制台黑窗、无需安装 Python
- Win7 双架构交付：
  - `vehicle_screening_win7_x64_setup.exe` / `_ia32_setup.exe`（NSIS per-user 安装包，免管理员）
  - `vehicle_screening_win7_x64_portable.zip` / `_ia32_portable.zip`（便携版）
  - `SHA256SUMS.txt` 校验和
- Node 后端（`desktop/server/`）：
  - SheetJS 解析 Excel（表头标准化、候选列匹配、无牌车过滤、原始列保留，1:1 移植）
  - 四种筛查模式（配对/绝对时间/频繁/重点人）+ 评分 + 分组展示，与 Python 版逐字段对拍通过
  - ExcelJS 导出（标题/摘要/冻结表头/自动筛选/合并单元格/风险底色）
  - 会话管理：内存 + `%APPDATA%\VehicleScreening` JSON 持久化，2 小时 TTL，重启可恢复
  - 卡口库/重点人库长期保存在用户数据目录（解决 Program Files 不可写问题）
- Electron 主进程：
  - 单实例锁（重复启动聚焦已有窗口）
  - 端口 11000 起自动顺延，仅绑定 127.0.0.1
  - 禁用 GPU 硬件加速（Win7 老显卡兜底）与沙箱（规避 Win10+ API 依赖）
  - 导出走原生保存对话框，导出完成可一键打开所在文件夹
  - 崩溃弹 Win32 错误框，不静默退出
- UI 重设计（亮色政务专业风）：
  - 墨蓝顶栏 + 青绿主色，风险红黄蓝仅用于数据行
  - 微软雅黑原生字体，零网络依赖
  - 全部颜色 oklch → hex（Chromium 108 兼容基线）
  - 高密度数据表格 + 吸顶表头 + 风险行底色（与导出 Excel 一致）
- 程序化生成应用图标（青绿圆角方块 + 道路 + 琥珀定位点）
- 测试基建：
  - `scripts/smoke-test.js` API 冒烟测试
  - `scripts/parity-test.js` Python↔Node 对拍（合成 254 行数据集，四种模式含跨天时段全部 PASS）
- 旧版 Python/Flask 实现（`app.py`、`templates/`、打包脚本）全部保留作参考，不参与构建

### 兼容性说明

- 32 位版受约 2GB 内存限制，建议单文件 10 万行以内；更大数据用 64 位版
- `pandas sort_values`（quicksort，不稳定）与 Node `Array.sort`（稳定）在相同评分行的先后顺序上可能不同，属展示顺序差异，不影响结果集合（对拍已按排序无关方式验证）
- `pandas value_counts` 并列计数的卡口拼接顺序为实现相关，Node 端统一为名称升序
